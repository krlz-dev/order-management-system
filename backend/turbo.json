{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["target/**"]
    },
    "test": {
      "outputs": ["target/surefire-reports/**"]
    },
    "clean": {
      "cache": false
    }
  }
}